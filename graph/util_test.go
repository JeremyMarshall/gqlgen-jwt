package graph_test

import (
	. "github.com/onsi/ginkgo"
	// . "github.com/onsi/gomega"
	// . "github.com/JeremyMarshall/gqlgen-jwt/graph"
	// "github.com/JeremyMarshall/gqlgen-jwt/graph/model"
	// "github.com/JeremyMarshall/gqlgen-jwt/rbac"
)

var _ = Describe("Util", func() {
	Describe("ConvertRole", func() {
		Context("Can convert from rbac to model", func() {
			It("should succeed", func() {

				// r := rbac.Rbac{
				// 	Permissions: []string{},
				// 	Parents:     []string{},
				// }
				// m := convertRole("test", m)

				// Expect(longBook.CategoryByLength()).To(Equal("NOVEL"))
			})
		})
	})
})
